<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidKotlinDebugging">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.HelloWorldActivity" android:exported="false"/>
        <activity android:name=".ui.NumSumActivity" android:exported="false"/>
        <activity android:name=".ui.CurrentDayActivity" android:exported="false"/>
        <activity android:name=".ui.RandomColorActivity" android:exported="false"/>
        <activity android:name=".ui.PrintListActivity" android:exported="false"/>

        <activity android:name=".ui.FavoriteNumberActivity" android:exported="false"/>
    </application>
</manifest>